/* const { Router } = require('express');
const router = Router();

const { userSing } = require("../controllers/authentication/userSing");


router.post("/login", userSing)
router.post("/register", userRegister)


module.exports = router; */